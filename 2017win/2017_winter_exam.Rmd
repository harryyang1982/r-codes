---
title: "2017-12-18 Final Exam"
author: "양승훈"
date: '2017 12 15 '
output: html_document
---

### 단답식

1. 단답식(20점)
    1. 파이프를 풀어내시오. (5점)

```{r echo=F, message=F, warning=F}
library(tidyverse)
```

    
```{r}
mpg %>% 
  group_by(cyl) %>% 
  filter(class == "compact") %>% 
  summarise(hwy_mean = mean(hwy))
  
```

    2. 아래의 코드를 파이프로 풀어내시오. (5점)
    
```{r}

summarise(group_by(filter(diamonds, price > 10000), cut), price_mean = mean(price))

```

    3. filter와 select의 차이점은? (5점)
    4. filter(DF, var1 %in% c(2,3)) 이 뜻하는 바를 간단히 쓰시오. (5점)

### 분석 문제

2. 데이터 분석 1 - (총 50점)
    1. seoul.csv를 로드하시오 (1점)
    2. 행정구역(NID2), 주택형태(SQ2), 주거점유형태(SQ3), 성별(SQ5), 혼인상태(SQ7), 종교(SQ8), 자신의 건강상태(A4_1), 삶의 만족도(A6), 스트레스(A8), 극장에서 영화관람 연간 총 액수(A13_1_11), 전시회관람 연간 총 액수(A13_1_2), 서울시민 자부심(A49), 보수진보(A52), 소득(DQ3_4)의 변수명을 적절히 바꾸시오 (3점)
    3. 행정구역에 따른 소득을 표로 만들어 데이터프레임으로 표현하시오. (3점)
    4. 소득에 따른 삶의 만족도를 표로 만들어 데이터프레임으로 표현하시오. (3점)
    5. 소득과 성별에 따른 삶의 만족도를 표로 만들어 데이터프레임으로 표현하시오. (5점)
    6. 스트레스에 따른 삶의 만족도를 표로 만들어 데이터프레임으로 표현하시오. (3점)
    7. 소득에 따른 극장에서의 영화관람 총 액수를 표로 만들어 표현하시오. (3점)
    8. 성별과 소득에 따른 극장에서의 영화관람 총 액수를 표로 만들어 표현하시오. (5점)
    9. 혼인상태에 따른 소득 수준을 표로 만들어 표현하시오. (3점)
    10. 종교에 따른 혼인상태를 표로 만들어 표현하시오. (3점)
    11. 지역별 혼인상태에 따른 소득 수준을 표로 만들어 표헌하시오. (5점)
    12. 1~9까지의 그래프를 모두 표로 표현하시오. (총 6점, 개별점수 있음)
    13. 극장에서 영화관람 연간 총 액수와 전시회관람 연간 총 액수 사이의 ‘상관관계’를 연산하고, ‘회귀분석’할 것. 이후 산포도를 그리시오. (총 7점, 개별점수 있음)
3. 데이트 분석 2 - 비정형 data (총 30점)
    1. 파일의 top_20을 추출 하시오. (10점)
    2. 빈도순으로 워드클라우드를 그려내시오. (15점)
    3. 기타 검토 사항 : 조사나 1글자 단어의 제거. (5점)
